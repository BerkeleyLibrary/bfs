services:
  bfs:
    build: .
    image: containers.lib.berkeley.edu/lap/bfs/development:latest
    init: yes
    volumes:
      - ./:/opt/app-root/src
      - ./secrets:/run/secrets:ro

version: "3.8"
